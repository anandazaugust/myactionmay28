name: myflow-4
on: 
  workflow_dispatch:
   inputs: 
      choose-env: 
       description: test
       required: true
       type: choice
       default: dev
       options:
        - dev
        - prod
        - uat

permissions:
 id-token: write

jobs: 
  devuatb: 
   runs-on: ubuntu-latest
   environment: dev
   steps:
     
     - name: get token
       
       run: az account get-access-token

     